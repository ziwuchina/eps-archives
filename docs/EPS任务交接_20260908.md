# EPS2016 逆向研究 — 任务交接文档（233 本机豆包 agent 接手）

> 生成时间：2026-09-08
> 交接方：本机（192.168.1.xxx 当前电脑）豆包 agent
> 接手方：233（192.168.1.233）本机豆包 agent
> 说明：本文件是当前任务全部状态的权威交接，接手方按此续跑，勿重复已完成的排查。

---

## 一、任务全景

用户目标：**把 EPS2016 这款软件"完完全全逆向研究"，并打通外部 COM 自动化通道**，最终为生产（顺德联合测绘、预售面积核算、EPS 工程批量处理）带来可用的自动化能力。

当前阶段核心：**EPS2016 官方 COM 外部自动化通道的完整闭环验证**
`CreateObject("Eps.EpsApplication")` → `GetScriptDispatch` → `InitWorkSpace(打开工程)` → `UpdateCurMap` → 建对象 → `SaveBufferObjToDatabase` → `CloseAllEdb`

并行要求：飞书看板（裴谦-EPS任务看板）同步、GitHub eps-archives 同步。

---

## 二、环境信息（233 本机）

| 项 | 值 |
|---|---|
| EPS 实际使用版本 | `D:\EPS2016Shunde\Eps.exe`（已从中文路径迁移，加密狗适配此版） |
| 另一版本（勿用） | `D:\EPS2026G\Eps.exe`（EPS2026） |
| 正确工程 | `D:\佛顺数据数据\顺德数据.edb`（9.5MB，用户手动打开成功） |
| 测试副本 | `D:\eps_com_test\test_shunde.edb`（9.5MB，全英文路径，从顺德数据复制） |
| 失效模板路径 | `D:\EP2010\system\template\7\房产成果顺德测绘\`（不存在）；`D:\深测\【测绘监理模板】\`（不存在） |
| 模板文件 | `佛山市基础地理模板_500.mdt`（顺德工程使用的模板） |
| 业务脚本目录 | `D:\AIcode\mcpida\wrapper\`（全部已部署脚本） |
| Python | `C:\Miniconda3\python.exe` |
| COM 注册 | Eps.EpsApplication CLSID `{AD80BF8A-74C4-44FB-AF57-B1BCA34D06C6}` LocalServer32=`D:\EPS2016Shunde\Eps.exe`（已正确） |
| WzExtBridge 日志 | `D:\EPS2016Shunde\Temp\wz_ext_bridge.log` |

---

## 三、已确认成果（勿重做）

### 3.1 COM 前置通道（已验证全通）
```
CreateObject("Eps.EpsApplication")        → OK
SetVisible(1)                             → OK
GetScriptDispatch("SScript.dll","SSProcess") → OK
GetScriptDispatch("SScript.dll","SSProcessView") → OK
GetScriptDispatch("SScript.dll","SSParameter") → OK
GetSysPathName(0)                         → D:\EPS2016Shunde（正确）
```
参考脚本：`D:\AIcode\mcpida\wrapper\com_test_session1.vbs`

### 3.2 InitWorkSpace 行为（已摸清）
- `InitWorkSpace "D:\佛顺数据数据\顺德数据.edb", ""` 返回 err=0
- 但**工程实际没有加载**（截图证据：窗口标题只有 `Eps2016(顺德基础测绘)`，无工程名；地图区空白；GetActiveMap=0 只是默认值）
- 弹"模板文件不存在"警告窗（工程内部记录的原始路径是 `D:/深测/【测绘监理模板】/fssj.edb`，该路径已失效）——点确定后继续
- 第二参传模板名 `佛山市基础地理模板_500.mdt` 可避免该弹窗，但工程仍不加载

### 3.3 建对象调用（全部失败，待攻克）
```
PushUndoMark                    → -2147417851 (0x80010105 RPC_E_SERVERFAULT)
CreateNewObjByCode(0)           → 同错
CreateNewObjByCode(1001/2001/3001/101/201) → 同错
```
所有 SSProcess 方法在 InitWorkSpace 后都返回 RPC_E_SERVERFAULT（除了 UpdateCurMap 和 GetSysPathName 返回 OK）。
**推断**：工程未真正打开导致 SSProcess 无操作上下文。

### 3.4 静态逆向资产（已完成）
- PE 分析：Eps.exe 2.54MB，x86 PE32，v3.1.1.1，编译 2018-10-07，421 导出，无 ASLR
- COM 类型库 dump：`D:\AIcode\mcpida\wrapper\COM_interfaces_dump.txt`（本机 `Eps2016_exe\`）— ISSProcess 468 方法、IEpsApplication 27、ISSProject 32
- CHM 文档全解析：`Help\EpsScipt-20161030.chm` → 611 htm → 479 方法 → `EPS2016_脚本API目录.xlsx` + `eps_api_catalog.csv`
- 官方 demo：`进程外调用EPS_Demo.vbs`（GBK 编码）
- 80 个 COM ProgID：EpsCom.* 系列 60+ 个

### 3.5 其他
- 环境迁移完成：EPS2016 → `D:\EPS2016Shunde\`，桌面快捷方式 `epsold.lnk`
- 飞书看板 27 条记录已重启更新
- GitHub eps-archives commit `6fe5f0f`（6 文件）

---

## 四、当前卡点与最可能的突破口

### 卡点
InitWorkSpace 返回 OK 但工程没真正打开 → SSProcess 无地图上下文 → 所有建对象/编辑调用 RPC_E_SERVERFAULT。

### 突破口（按优先级）
1. **全英文路径测试**（脚本已备好，未跑）：`ascii_test.vbs` 用 `D:\eps_com_test\test_shunde.edb` + 空模板名。若英文路径能打开工程（窗口标题显示工程名），则证明是中文路径在 COM 通道的编码问题。
2. **对比手动打开**：用户手动打开顺德数据.edb 成功（窗口标题 `Eps2016(顺德基础测绘) - D:\佛顺数据数据\顺德数据.edb - [佛山市基础地理模板_500.mdt]`）。COM 与手动打开的差异点要找出来。
3. **GetScriptDispatch 参数**：SSProject 用 `GetScriptDispatch("SSProject.dll","SSProject")` 返回 Nothing（之前"OK"是 On Error 假象）。可试 `CreateObject("SSProject.SSProject")` 或正确 ProgID。
4. **IEpsApplication.ExecuteSDLFunction / SendFrameMessage / SendViewMessage**：可尝试绕过 SSProcess 直接发命令。

---

## 五、关键脚本清单（233 `D:\AIcode\mcpida\wrapper\`）

| 脚本 | 用途 | 状态 |
|---|---|---|
| `com_test_session1.vbs` | COM 前置通道测试 | ✅ 已通 |
| `full_test.vbs` | InitWorkSpace 新建工程测试 | 卡弹窗（已废弃路径） |
| `open_test.vbs` | InitWorkSpace 打开 fssj.edb | 卡弹窗（已废弃路径） |
| `clean_test.vbs` | 干净环境打开顺德数据测试 | InitWorkSpace 返回 OK 但工程不加载 |
| `full_loop.vbs` | 完整闭环（建对象） | 建对象全 RPC_E_SERVERFAULT |
| `gbk_test.vbs` | GBK 编码中文路径测试 | InitWorkSpace OK，建对象仍 FAULT |
| `deep_diag.vbs` | 深度诊断（SSProject） | SSProject Nothing，建对象 FAULT |
| `fine_diag.vbs` | 精细诊断（每调用独立输出） | 同上 |
| `verify_open.vbs` | 验证工程是否真打开（截图留 30s） | ✅ 截图证明工程未加载 |
| `ascii_test.vbs` | **全英文路径测试（未跑）** | 待执行 ← 下一步 |
| `eps_click_ok.py` | 自动点"确定"弹窗 | ✅ 有效（需 ScheduledTask 跑） |
| `eps_disk_check.py` | 磁盘满误报检查 | ✅ 已排除（578GB 可用） |

### ScheduledTask Interactive 模式（233 上跑 GUI 相关脚本的唯一方式）
```powershell
$action = New-ScheduledTaskAction -Execute 'cscript.exe' -Argument '//nologo D:\AIcode\mcpida\wrapper\xxx.vbs'
$principal = New-ScheduledTaskPrincipal -UserId 'Administrator' -LogonType Interactive -RunLevel Highest
Register-ScheduledTask -TaskName 'EpsXxx' -Action $action -Principal $principal -Force | Out-Null
Start-ScheduledTask -TaskName 'EpsXxx'
```
**关键**：SSH 会话是 session0，看不到 session1 桌面；截图/点窗口必须用 ScheduledTask Interactive。

---

## 六、已知坑（勿重试）

| 坑 | 说明 |
|---|---|
| SSH 中文乱码 | 233 上中文输出必须"Python 写 UTF-8 文件 → scp 取回 → 本地 Read"；VBS 脚本必须 **GBK 编码**（`CreateTextFile(..., True)` 写 UTF-16 也可） |
| 命令框 VBSExecute 注入 | 4 种命令格式均被 EPS 路由层丢弃，不可行 |
| taskkill 杀 COM 僵尸 | `Eps.exe -Embedding`（DCOM 激活）taskkill/WMI 拒绝访问，只能重启 233 或手动关窗口 |
| SSH 截图全黑 | session0 无桌面，必须 ScheduledTask Interactive |
| "磁盘已满"弹窗 | 误报，D 盘 578GB 可用；多 EPS 实例资源冲突导致 |
| GetObject 连已运行实例 | 429 错误，EPS 未注册 ROT，必须 CreateObject 新建 |
| SSProject dispatch | `GetScriptDispatch("SSProject.dll","SSProject")` 返回 Nothing |
| GetCurMapName | 方法不存在（438），正确名是 GetMapStatus/GetActiveMap |

---

## 七、CHM 官方 API 关键信息（建对象配方）

```
SSProcess.PushUndoMark
SSProcess.CreateNewObjByCode 0        ' 示例用编码 0
SSProcess.AddNewObjPoint 1000.01, 1000.34, 22.45, 0, ""
SSProcess.AddNewObjToSaveObjList
SSProcess.SaveBufferObjToDatabase
```
- 有效编码示例：0（临时要素）、3、831000（高程点）
- 查有效编码：`SSProcess.FindFeatureCode("SSObj_Type==POINT")`（当前环境返回 449 参数错误，待查签名）
- 完整 479 方法：`EPS2016_脚本API目录.xlsx` / `eps_api_catalog.csv`
- COM 方法名以 `COM_interfaces_dump.txt` 为准（CHM 文档名 ≠ COM 实际名，如 GetSystemPathName≠GetSysPathName）

---

## 八、飞书看板 & GitHub 状态

- 看板：base `QYr1bFaZnarOR6sIrmOc0AqknVM`，表 `tblXUd9D9byrNdJZ`，视图 `vewgAbiomJ`，27 条记录
- GitHub：`ziwuchina/eps-archives`（公开 master），本地仓库 `C:\Users\Administrator\Desktop\AI相关\EPS项目汇报\eps-archives\`
- 本交接文档应同步推送 GitHub + 看板新增一条记录

---

## 九、接手后的下一步行动（建议顺序）

1. 跑 `ascii_test.vbs`（全英文路径测试）——确认/排除中文路径编码问题
2. 若英文路径能打开工程 → 用英文路径工程完成完整闭环（建对象+存库）
3. 若不能 → 对比手动打开差异，查 SSProject 正确获取方式，或试 `ExecuteSDLFunction`/`SendFrameMessage`
4. 闭环打通后 → 封装成"批量打开工程→处理→关闭"的生产工具脚本
5. 更新看板 + GitHub（每有进展同步一次）
