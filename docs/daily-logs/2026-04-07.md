## EPS 自动化调试（曾工）- 持久结论
- 用户目标明确：要验证/实现 BAR 或 VBS 能力，不接受用 Ctrl+V 代替能力验证。
- 关键限制1：`$epsscript,...` 通过 EPS 命令栏（BAR command input）不稳定，常见报错为 `Load ...SDL/(.DLL) failed`。
- 关键限制2：`SSProcess.RunScript` 的 `ret=0` 不能单独作为成功证据；存在"脚本未实际执行/甚至脚本不存在也返回0"的假成功现象。
- 稳定性要点：EPS 重启后 PID/句柄会变；自动化必须每次重定位主窗口与工具栏，不能复用旧句柄。
- 工具栏要点：v23 工具栏窗口可能存在但可见性会变化（隐藏/浮动）；应按同 PID 绑定并做可见化处理，不用绝对屏幕坐标。
- 本轮已形成共识：需要做底层分析（逆向/调用链分析）来确定 `$epsscript`/`RunScript` 的真实可用入口，用户已明确同意（"点头"）。

## EPS 自动粘贴链路收敛（14:42）
- 关键结论：`RunScript/MapMethod("RunScript", ...)` 的 `ret=0` 不能作为业务成功证据；已用探针脚本验证其可能出现"接口成功但脚本体未执行（OnInitScript/OnClick 未触发）"。
- 手动粘贴有效的根因：手动点击走的是 EPS UI 工具栏/菜单命令链路，与 `RunScript` 不是同一执行路径。
- 已定位并验证可用自动化入口（非 Ctrl+V）：向 EPS 主窗口发送 `WM_COMMAND`，`Paste=57637`（同时确认 `Copy=57634`、`Cut=57636`）。
- 已落地工具：`D:\AIcode\mcpida\tools\eps_edit_command.py`，支持 `--action copy|paste|copy-paste|cut`，并内置 EDB 变更校验（mtime/size）。
- 实测通过：用户手动复制后，执行 `python D:\AIcode\mcpida\tools\eps_edit_command.py --action paste --delay 1.0` 返回 `edbChanged=true`，当前工程 `..._500-27.edb` 的 mtime 发生变化，且 EPS 进程数本次执行前后稳定（未新增）。
- 过程性结论：`AddClipBoardObjToMap` 经 COM 调用虽可 `ret=0`，但在当前环境未稳定体现业务落地；UI 命令链路是当前可复现、可交付方案。

<!-- EOD:2026-04-07:START -->
## End of day
- 全天聚焦 EPS 自动化调试（曾工需求）
- 核心发现：RunScript 接口返回 ret=0 不可靠，存在假成功
- 突破：定位到 WM_COMMAND 命令字（Paste=57637）作为稳定入口
- 交付：eps_edit_command.py 工具落地，实测 EDB 变更可验证
- 共识：底层分析（逆向/调用链）确认真实入口，用户已同意
<!-- EOD:2026-04-07:END -->
