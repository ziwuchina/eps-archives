# 2026-04-08

## EPS 监控项目关键决策
- 曾工明确纠偏：监控的核心目标是"溯源 + 后台数据监管"，不是"自动压问题/自动处理业务"。
- 监控分层原则确定：
  - `observe-only`（默认）：只采集、只审计、不执行动作；
  - `observe+act`（后续可选）：执行层策略处理，必须与监控层隔离。
- 曾工确认"全面监控"按 7 项推进：5 项在线采集（进程/UI/输入/命令/网络）+ 2 项离线溯源（VBS 来源、DLL/SDL 来源）。

## 当日落地成果（可复用）
- 已上线实时监控与可视化：
  - 终端看板：`D:\AIcode\mcpida\monitoring\live_watch.py`
  - 本地网页仪表盘：`D:\AIcode\mcpida\monitoring\web_dashboard.py`（默认 `http://127.0.0.1:17888`）
- 已提供单入口一键编排脚本：`D:\AIcode\mcpida\tools\eps_onecmd.py`
  - 支持：统一启动监控/仪表盘、执行 BAR 命令、状态查询、重启/停止服务。

## 关键技术结论（EPS 导出能力）
- "Edb 属性/扩展属性导出"当前是"部分可用"：
  - 直接命令 `'$sdl,ssdataxinterface,export.externfile`（BAR id=17）当前失败，出现 `Load $sdl.SDL/(.DLL) failed`。
  - 脚本导出链路可用（示例 BAR id=25：`地籍处理\输出宗地楼幢示意图.vbs`，执行结果 `ok=true`）。
- 持续有效结论：导出元素属性更可靠路径是 `SSProcess.SetDataXParameter + ExportData` 流水线，而非单一命令字符串。

## 证据文件（本日）
- 弹窗来源双通道分析：`D:\AIcode\mcpida\reports\popup_source_channels_20260408_1138.json`
- 导出失败样本：`D:\AIcode\mcpida\reports\bar_unified_exec_20260408_124052_id17.json`
- 导出成功样本：`D:\AIcode\mcpida\reports\bar_unified_exec_20260408_124309_id25.json`

## EPS 脚本调用新结论（补充）
- 用户再次确认 EPS 自动化脚本调用逻辑：EPS 脚本在运行时 VBS 脚本需要稳定路径。
- 已验证结论：`SSProcess.MapMethod("RunScript", <path>)` 可返回 `0` 表示成功，但测试 `Custom\test_write.vbs` 和 `Custom\export_selected_zd_line_xyz_attr.vbs` 未生成预期文件，说明当前路径在当前上下文"加载脚本"是可靠的，在 `OnClick()` 执行。
- 稳定性判断结论：需要把"脚本实际路径"中的 EPS 相对路径作为参数 BAR 按钮事件/脚本调用格式，而不是直接调用 COM 对象来执行脚本。

## 自动化路径参数化与可追溯：
- 已落地 `D:\AIcode\mcpida\tools\bar_unified_executor.py`：
  - `_run_bar_catalog` 新增 `use_normalized` 参数；
  - `script` 类型计划任务默认 `useNormalized: false`；
  - 执行阶段根据配置透传 `use_normalized`，确保脚本命令不被 normalized 前缀覆盖原始语义。
- 该更新旨在解决 `$epsscript` 参数在脚本调用中的语义丢失问题。

<!-- EOD:2026-04-08:START -->
## End of day
- EPS 监控项目方向确认：溯源 + 后台数据监管（非自动压问题）
- 监控分层原则：observe-only（默认）与 observe+act（可选，需隔离）
- 全面监控 7 项推进：5 项在线采集 + 2 项离线溯源
- 实时监控看板上线：live_watch.py + web_dashboard.py (port 17888)
- 单入口编排脚本：eps_onecmd.py（启动/状态/BAR 命令/重启）
- EPS 导出结论：Edb 属性导出部分可用；脚本导出链路可靠 (BAR#25 成功，BAR#17 失败)
- 可靠路径确认：SSProcess.SetDataXParameter + ExportData 流水线
- 自动化路径参数化：bar_unified_executor.py 新增 use_normalized 参数
<!-- EOD:2026-04-08:END -->
