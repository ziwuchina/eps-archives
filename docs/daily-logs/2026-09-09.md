# 2026-09-09 工作日志（233）

## 一、步骤2.4 调用链闭环（已完成）
- 静态反汇编 SSProject.dll（x86, ImageBase 0x10000000, 1267 导出, 无 ASLR）：ExecuteSDLCommand(0x1004D5B6) → ExecuteFunction → IsRegisterCmd(0x1005F6C8) → CSDLInterface vtable[0x40] 虚调用派发（未命中走 OnNewDlgBase 对话框）。
- 官方文档对照命中：SSProcess.ExecuteSDLFunction（CHM 296.htm）、ExecuteCommand(libFileName, procName, desc, transSelection) 外部模块接口（=WzExtBridge 签名）。
- 无 ASLR → 静态 RVA 即动态偏移，对齐闭合；frida attach 13008/19604 被拒（用户进程未提权）已记录。
- 看板 recvufViXukJxm 回填已完成，回读 PASS。

## 二、阶段3.1 函数批量分析与模块切片（已完成，254 函数全量）
- 批次：批1 SSFunc(26) / 批2 SSProject(26) / 批3 SSView+SSArray+SSParameter(28) / 批4-5 SSProcess 编辑操作 A+B(53) / 批6-10 SSProcess 剩余(121)。
- 交叉核对：官方帮助目录 SSProcess 174 函数 100% 覆盖（CSV 遗漏 48 个已补齐，含 ExecuteCommand/ExecuteSql/ImportMdbTable）；CSV 独有 209 个属 CHM 其他章节，记已知差异。
- 本次宗地绘制/录入全部已用 API 官方语义闭环（CreateNewObjByCode/AddNewObjPoint/SaveBufferObjToDatabase/SetNewObjValue 等）；GetObjectAttr/GetSelGeoValue err=450 差异确认（读属性走 SQL）。
- 看板 recvufViXuRMRh 回填已完成，回读 PASS。

## 三、WzExtBridge 调用链验证（已完成，静态）
- WzExtBridge.SDL 实为 PE 模块（299KB, 32 位 GUI, 2026-04-12 编译, ASLR）。
- 12 导出 = 完整外部模块接口（RegisterCmd/ExecuteCommand/ExecuteSDLFunction/SetModulName/GetSDLCommandInfo/WzExtProbe 等）。
- WzExtProbe(RVA 0x4CC0) 反汇编：3 栈参与官方 proc(CStringArray*, CGeObjList*, CProgressCtrl*) 完全一致。
- 看板 recvufViXuLAsR 回填已完成，回读 PASS。

## 四、ScriptCryptor 加密授权体系（已完成，静态）
- 加密链：ScriptCryptor.exe → EncryptByPI.dll(COM) → PiEncrypt.dll/PiEncrypt1.dll（多密钥版本）→ libcrypto-1_1(OpenSSL 1.1)。
- License 格式破解：模块名,模块中文名,<16进制权限码>,<32hex MD5>（SSEdit/SScript/SSCheck 按模块授权）；UsbKey=SafeNet HASP 加密狗。
- 看板 recvuhA1zU4pEN 回填已完成，回读 PASS。

## 五、看板治理（17 进行中 → 3）
- 实质完成 3 条（WzExtBridge/ScriptCryptor/阶段2 API 目录）+ 归档 11 条（被官方 COM 通道取代）。
- 用户决定搁置 2 条 EPA API 任务（已阻塞，恢复条件=业务需求）。
- 进行中仅剩 1 条（EPS 任务交接 git push）。

## 六、git push 同步（本次提交）
- 确认 eps-archives 远程仓库（HEAD f3272df 为 2026-09-08 交接提交）；本轮增量同步 31 文件 + 2 脚本 + 本日志。
