# 2026-04-10

## EPS WM_COMMAND 抓包项目（今日主线）

### 背景
- 曾工要求在 IDA 调试器里用 F9 恢复 EPS 运行，然后点「佛山数据.edb」进入工作空间，再点「宗地默认信息填写」工具栏按钮
- EPS 是在 IDA 调试器里启动的（`ida.exe pid=11792`），调试器需要处于运行状态才能让 EPS 正常工作
- 曾工多次说"继续"，说明要在之前的成果上继续推进

### 今日成果

#### 1. Frida WM_COMMAND 抓包（成功）
- Frida 成功 attach 到 EPS 进程（pid=11912，`D:\AIcode\mcpida\tools\tmp_sim_click_capture_upgrade.py`）
- Hook 了 `SendMessageW(0x762064d0)` 和 `PostMessageW(0x76206280)`
- **核心发现**：点击「宗地默认信息填写」按钮，触发的是：
  ```
  msg=0x111 (WM_COMMAND)
  wParam=0x00000001 → BN_CLICKED(0) + button_id=1
  ```
  - 多次点击（11:40、11:44、11:51、11:53）全部命中 `button_id=1`，完全确认
  - lParam（发送者句柄）每次不同，说明经过多层窗口路由

#### 2. 调试器状态
- `dbg_continue`（IDA F9 等效）：返回 `continued: true`，但断点从未停住
- `dbg_callstack`：返回空，说明调试器在运行但没有断点命中
- 原因：断点设在了 IDA 可见地址（thunk 函数），实际执行路径可能没经过那些地址
- IDA MCP 当前端口：10000（不是 11339），`list_instances` 显示 `pid=11792, port=10000`

#### 3. BAR 文件分析（重要线索）
- `D:\EPS2026G\DeskTop\顺德基础测绘\InterfaceSettings_玮哥宗地v23.bar`
- 「宗地默认信息填写.vbs」对应 BAR 路径：`.\玮哥专用脚本\宗地默认信息填写.vbs`
- v23 有 45 个 commands，49 个 labels
- 当前测试过的 BAR ID：id=7（画圆.vbs，压测不稳定），id=13/14/33（ERP auth，通过）

#### 4. 工具栏按钮矩阵（4/7）
- 测试过：id=13,14,33(PASS), id=31,7(SOFT_FAIL)
- 4/8-4/9 待测试：id=1,2,3（从文件名 `bar_unified_exec_20260407_175128_id1.json` 等可知存在）
- 需要确认哪个 ID 对应"宗地默认信息填写"

#### 5. IDA 函数列表（与按钮相关）
- `CBCGPToolbarCustomize::OnCommand` — 0x42BFF6（thunk）
- `CBCGPToolBar::OnCommand` — 0x42BCDE（thunk）
- `CFrameWnd::OnCommand` — 0x42AD24（thunk）
- `CSSView::OnCommand` — 0x42A81A（thunk）
- 实际实现均在 `__imp_` 里，需要在断点触发时查看调用栈

### 待办
1. **最高优先**：找到"宗地默认信息填写"的 BAR 命令 ID
   - 方案A：IDA 设断点 `CBCGPToolBar::OnCommand`，点击按钮，看调用栈
   - 方案B：直接跑 `bar_unified_executor.py` 逐个 ID 试（已有 id1,id2,id3 的 report）
2. **调试器断点**：曾工需要在 IDA 里手动设断点（`Ctrl+F` → `OnCommand` → `F2`）并点击按钮触发
3. **Frida 内存搜索**：`Module.enumerateRangesSync` API 在 Frida 17.8.2 有兼容问题（报错：not a function），无法搜索 EPS 进程内存中"宗地默认信息填写"字符串

### 关键文件
- `D:\AIcode\mcpida\tools\tmp_sim_click_capture_upgrade.py` — Frida WM_COMMAND 抓包脚本
- `D:\AIcode\mcpida\reports\tmp_sim_click_capture_upgrade.jsonl` — 抓包结果
- `D:\AIcode\mcpida\reports\bar_triage_wei_ge_zongdi_v23.json` — v23 BAR 文件分析
- `D:\AIcode\mcpida\reports\ida_dbg_events_live.jsonl` — IDA 调试事件日志
- `D:\EPS2026G\DeskTop\顺德基础测绘\InterfaceSettings_玮哥宗地v23.bar` — 宗地 BAR 文件

### 曾工习惯
- 表达简洁，说"继续"意味着接着上次的成果推进
- 遇到问题会明确告知"你自己想办法"
- 已在 IDA 里按过 F9 且 EPS 正常运行
