# 2026-09-05 工作日志

## 一、EPS2016 环境迁移（中文路径 → 英文路径）

**背景**：EPS2016 长期使用中文安装路径 `D:\EPS2016顺德专用含不动产外网-FS2K20181118-E1022`，导致脚本/工具路径处理困难。用户决定迁移到全英文路径，并确定 EPS2016（顺德专用版）为唯一工作版本（加密狗适配此版本）。

**执行结果**：
- 迁移后主程序：`D:\EPS2016Shunde\Eps.exe`（540 项完整，含 WzExtBridge.SDL / Hello.SDL / nethasp.ini）
- 桌面快捷方式：`Eps快捷.lnk` → **`epsold.lnk`**（指向 `D:\EPS2016Shunde\Eps.exe`）
- 误移目录已恢复：`D:\EPS2016不动产顺德专用外网授权版`（630 项，另一份完整安装，无 WzExtBridge.SDL）
- EPS2026 未动：`D:\EPS2026G`（快捷方式保持 `Eps.exe - 快捷方式.lnk`）
- 启动验证通过：显示山维科技欢迎界面，无授权/加密狗错误

**配置扫描结论**：EPS2016 主配置（EpsGlobal.ini 等）不硬编码安装路径，迁移无影响；仅 2 个 `Eps.ini` 有历史 LastUseFile 记录（指向旧 E 盘路径，无害）。

## 二、$SDL 加载机制验证成功（WzExtBridge 关键突破）

**事件**：EPS2016 执行 `$SDL,WzExtBridge,WzExtProbe` 命令被消费，**触发 WzExtBridge.dll CreateInterface 调用**。

日志证据（`D:\EPS2016Shunde\Temp\wz_ext_bridge.log`）：
```
[2026-09-04 23:51:40] CreateInterface called, context=00000000, detail=v-restored-returns-one
```

**结论**：
- `$SDL,模块,函数` 是 EPS 调用 SDL 扩展模块的有效命令格式
- EPS 内置 SDL 加载逻辑，.SDL 文件（或同名 DLL）是扩展载体
- WzExtBridge.dll 可被 EPS 正式加载并初始化

**附带根因定位**：命令"时灵时不灵"的真正原因是 **EPS 被模态对话框阻塞**（Load ver.SDL failed 弹窗、SDL 打开对话框、属性设置对话框），关闭后命令立即恢复。

## 三、EPS.exe 完整逆向研究启动

用户要求对 EPS2016 主程序 `D:\EPS2016Shunde\Eps.exe` 进行完整逆向。规划如下：

### 逆向目标
1. **程序结构**：PE 节区、编译器、导入/导出、字符串
2. **命令分发链**：Edit → OnEpsCommand → OCB → EXECFN 完整逻辑
3. **$SDL 命令解析**：EPS 解析 `$SDL,模块,函数` 的代码路径
4. **SDL 模块接口**：CreateInterface / RegisterCmd / ExecFunction 调用协议
5. **模块加载机制**：.SDL / .DLL 注册与加载
6. **授权/加密狗**：UsbKey / nethasp 检查逻辑
7. **VBS 引擎**：SScriptCore.dll（B 阶段2）

### 已有基础
- 命令分发链已定位：Edit→OnEpsCommand→OCB→EXECFN
- $SDL 命令格式已验证有效
- 17 个已加载模块清单（SSProject/SSEdit/SScript 等）
- Frida 工具链稳定（attach/spawn/hook）
- 干扰进程已清理（16592 等退出，仅剩 19028/11608 空壳 stub）

### 工具链
- IDA 9.0（233 本机）
- Frida（动态 hook）
- Python + 自研脚本（233 D:\AIcode\mcpida\wrapper\）

## 看板更新
- `recvufViXuLAsR`（WzExtBridge 开发）：更新 $SDL 验证突破
- `recvufViXuEqa5`（SDL 封装）：更新 $SDL 格式确认
- 新增 `recvuhtNRCr4Tc`（环境迁移，已完成）
