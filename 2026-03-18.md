# 2026-03-18

- IDA MCP session: analyzed SSCore32.dll.i64. CAPIDecrypt/Encrypt and LongString are XOR + hex; no 32-byte key derivation there. GetCefKeyString uses IsEncryptCoord and global table dword_101BAC80 (0x38-byte entries) with count dword_101BAC84; Get/Set/FindEncryptCoordSeedID read these globals; no writes found in SSCore32.dll (globals appear 0xFFFFFFFF). Seed list also parsed via EnableSeedID in sub_10075B6A; initialization chain includes sub_1003741A -> sub_10075B6A.
- Eps.exe.i64 via MCP: no EnableSeedID/SunwaySurveyCefKey strings; only CSSDatabase::IsEncryptCoord (member read). Indicates globals likely initialized in other module or at runtime.
- User needs step-by-step IDA debugging to set hardware write breakpoints at 0x101BAC80/0x101BAC84; MCP cannot control UI, only read IDB. User requested guidance to select Local Windows debugger and add breakpoints.
