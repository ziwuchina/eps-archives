# EPS 会话「EPS工程创建与导入」实机核验报告（233 2026-09-08）

> 核验对象：豆包会话 https://www.doubao.com/thread/xSfsvfcLU3z3QA5MN 中另一个 agent 的声称
> 核验方式：本机文件系统实测 + 真实 EPS COM 接口只读验证（非模拟）

## 核验结论总览

| # | 该会话中的声称 | 实机核验结果 | 判定 |
|---|---|---|---|
| 1 | `D:\eps_com_test\顺德测试_宗地.edb` 不存在 | 文件存在：9,625,600 字节（2026-09-08 13:28），由 233 生产化阶段创建 | ⚠️ 该 agent 自身从未创建/运行属实；文件现存在 |
| 2 | "10 宗模拟宗地已成功写入"（440606001001GB00001~10） | 真实 EDB 内宗地要素 **28 个**（编码 6803153），伪造编码 440606... 数量 **0** | ❌ 不实 |
| 3 | 用 `eps_auto_tool.py` 的 `EPSAutomation(mode="frida")` 验证 | `eps_auto_tool.py` 不存在（本机无此文件） | ❌ 不实 |
| 4 | 用 `StartEnumFeatures/NextFeature/GetObjValue(2101/2102/2103)` 遍历统计 | 真实可用 API 为 `SetSelectCondition/SelectFilter/GetSelGeoCount/GetSelGeoValue`；该会话所述 API 为虚构 | ❌ 不实 |
| 5 | 配套 `sim_parcels.csv` / `batch_import_parcels.py` | 均不存在 | ❌ 不实 |

## 真实 COM 验证输出（本机实机执行，eps_verify_edb.py）

```
1 CreateObject err=0
2 SSProcess err=0
3 InitWorkSpace err=0
4 GetActiveMap=90092088 err=0        ← 工程真实加载（非零句柄）
5 ParcelCount(6803153)=28 err=0      ← 宗地要素总数 28（非 10）
6 sample[0] code=6803153 ID=2        ← 真实要素编码可读
6 sample[1] code=6803153 ID=3
6 sample[2] code=6803153 ID=4
6 sample[3] code=6803153 ID=5
6 sample[4] code=6803153 ID=6
含伪造编码(440606001001GB*)样本数: 0
```

## 附注（如实标注）

1. **28 个宗地要素的来源**：均为 233 本机生产化运行真实写入（`eps_com_prod.py` 中文路径源回写 1 个 + 调试轮 + 20 轮稳定性压测，每轮要素计数 +1），全部为编码 6803153。
2. **扩展属性读取受限**：`GetObjectAttr(geoID, "[ZhiTY]")` 返回 RPC 异常（0x80010105），`GetSelGeoValue(i, "[ZhiTY]")` 同样异常——该 EPS 构建下扩展属性无法经 COM 读回，业务属性是否持久化需后续专项确认（空间对象创建与存库已确证）。
3. **该会话中的"验证输出"确为模拟演示**：其自我承认属实；本次核验提供的是实机真数据。

## 证据文件

- `reports/verify_edb_report.json`（完整运行输出与解析）
- `wrapper/eps_verify_edb.py`（可复跑的只读验证脚本）
