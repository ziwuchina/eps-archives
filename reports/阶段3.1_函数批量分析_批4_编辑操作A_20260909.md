# 阶段3.1 函数批量分析与模块切片 — 第四批（SSProcess 编辑操作A）报告（2026-09-09）

## 1. 批次规划进度

| 批次 | 对象类 | 函数数 | 状态 |
|---|---|---|---|
| 批1 | SSFunc | 26 | ✅ |
| 批2 | SSProject | 26 | ✅ |
| 批3 | SSView+SSArray+SSParameter | 28 | ✅ |
| **批4（本次）** | **SSProcess 编辑操作A** | **28** | ✅ |
| 批5+ | SSProcess 剩余子类 | ~308 | 待办 |

## 2. 第四批：SSProcess 编辑操作类 A（创建/保存/删除/撤销/变换）28 函数

### 2.1 功能聚类

| 类别 | 函数 | 说明 |
|---|---|---|
| 创建 | `CreateNewObj(type)`、`CreateNewObjByClass(fontclass)`、`CreateNewObjByCode(code)` | 按类型/字类/要素编码建对象 |
| 加点/入列 | `AddNewObjPoint(x,y,z,pointType,name)`、`AddNewObjToSaveObjList()`、`AddSelGeoToSaveGeoList(index)`、`AddSelNoteToSaveNoteList(index)` | 新对象加点、加入保存列表（图形+注记） |
| 保存/撤销 | `SaveBufferObjToDatabase()`、`PushUndoMark()` | 缓冲入库、撤销标记 |
| 删除 | `DeleteObject(geoID)`、`DeleteSelectionObj()`、`DeleteLayer(layerName)`、`DelSelGeo(index)`、`DelSelNote(index)`、`DeleteSelGeoPoint(index,pointIndex)`、`DeleteSelNotePoint(index,pointIndex)`、`DeltetNewObj()` | 对象/选择集/图层/点删除 |
| 属性变更 | `ChangeSelectionObj()`、`ChangeSelectionObjAttr(attrField,attrValue)`、`SetChangeSelectionParameter(matchConditions,updateSetAttrs)`、`ClearChangeSelectionParameter()` | 批量属性修改（条件匹配+更新集） |
| 变换/修饰 | `OffsetSelectionObj(x,y,z)`、`RemoveSelectionObjPoint(index,count)`、`ExplodeSelectionObj(explodeWay,delSrcObj,callbackFunctionName)`、`FilterSelectionObjVertex(miniDistV,miniDistH,...)` | 平移、去点、炸开、顶点过滤 |
| 锁定 | `LockSelGeoPoint(index,status)`、`LockSelNotePoint(index,status)`、`LockGeoPointsInMemory()` | 图形/注记点锁定 |

### 2.2 与自动化主线的直接对应（本次 12 宗地绘制已用 API 的官方语义）

| 主流程调用（eps_parcel_batch） | 官方签名（本批补全） | 语义 |
|---|---|---|
| `CreateNewObjByCode 6803153` | `CreateNewObjByCode(code)` | 按要素编码 6803153（所有权宗地）创建 |
| `SetNewObjValue` ×17 | 见批5（属性写，本批未含，下批补） | 属性字段写入 |
| `AddNewObjPoint` ×5 | `AddNewObjPoint(x,y,z,pointType,name)` | 5 点闭合环逐点添加 |
| `AddNewObjToSaveObjList` | `AddNewObjToSaveObjList()` | 加入保存缓冲列表 |
| `SaveBufferObjToDatabase` | `SaveBufferObjToDatabase()` | 缓冲入库（计数 28→40 的关键调用） |
| `PushUndoMark` | `PushUndoMark()` | 撤销标记（绘制前打点） |

### 2.3 数据源说明

- 23 个来自 `eps_api_catalog.csv`（CHM 611 页解析）；**5 个 CSV 遗漏**（`DeltetNewObj`/`SetChangeSelectionParameter`/`ExplodeSelectionObj`/`FilterSelectionObjVertex`/`LockGeoPointsInMemory`）已从官方帮助目录 `eps_script_help\...\编辑操作\` 直读补全 → CSV 编辑操作类覆盖率 ≈ 82%（23/28），解析器对 5 个页面标题存在遗漏，后续批次交叉核对官方目录。

## 3. 证据与复现

- 产物：`reports/stage31_batch4_SSProcess_edit.json`（28 函数：页号/名称/签名/标题/说明/CHM 原文摘录）
- 复现：`stage31_batch4.py` + `stage31_batch4_fix.py`

## 4. 下一步

- 批5：SSProcess 编辑操作 B（属性写/读：`SetNewObjValue`/`SetObjectAttr`/`GetSelGeoValue` 等 ~25 个，含本次属性录入 17 项的官方语义）；
- 随后按子类继续：数据库操作(24) → 选择集操作 → 数据整理 → 坐标转换 → 外部函数 → 系统设置 → 数据检查 → 图幅打印 → 地模处理。
