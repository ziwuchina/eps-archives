# 阶段3.1 函数批量分析与模块切片 — 批6-10（SSProcess 剩余全量）报告（2026-09-09）

## 1. 批次规划进度（最终）

| 批次 | 对象类/子类 | 函数数 | 状态 |
|---|---|---|---|
| 批1 | SSFunc | 26 | ✅ |
| 批2 | SSProject | 26 | ✅ |
| 批3 | SSView+SSArray+SSParameter | 28 | ✅ |
| 批4 | SSProcess 编辑操作A | 28 | ✅ |
| 批5 | SSProcess 编辑操作B | 25 | ✅ |
| 批6 | SSProcess 数据库操作 | 24 | ✅ |
| 批7 | SSProcess 数据整理 | 28 | ✅ |
| 批8 | SSProcess 系统设置+图幅打印 | 27 | ✅ |
| 批9 | SSProcess 选择集+地模+外部函数 | 23 | ✅ |
| 批10 | SSProcess 工程+数据转换+检查+坐标+角度 | 19 | ✅ |

**合计：10 批 254 个函数全量分析。**

## 2. 各批功能摘要

### 批6 数据库操作（24 个：数据库操作）

`AccessIsEOF`、`AccessMoveFirst`、`AccessMoveLast`、`AccessMoveNext`、`AccessMovePrev`

`AccessMove`、`AddAccessRecord`、`CloseAccessMdb`、`CloseAccessRecordset`、`CreateAccessMdb`

`CreateAccessTable1`、`CreateAccessTable`、`DelAccessRecord`、`ExecuteAccessSql`、`ExportSelectionToAccessTable`

`GetAccessFieldInfo1`、`GetAccessFieldInfo`、`GetAccessRecordCount`、`GetAccessRecord`、`GetAccessTableNames`

`IsExistentTable`、`ModifyAccessRecord`、`OpenAccessMdb`、`OpenAccessRecordset`


### 批7 数据整理（28 个：数据整理）

`ChangeCodeCopySelectionObj`、`ClearSelectionObjNodeSymbol`、`CloseSelectionObj`、`CreateHouseHeightNote`、`CreateHouseHeightPoint`

`ExecuteSql`、`IdentifySelectionObj`、`ImportMdbTable`、`InsertWaterLevelPoint`、`LinkNearNoteObj`

`MapMethod`、`MergeIslandAreaObj`、`MergeNearGeoObj`、`MergeNearLineObj`、`MergeNearNoteObj`

`MergeNearPointObj`、`MergeObjPointList`、`ObjectDeal`、`RepairDGXHeight`、`RepairSelectionObjPointList`

`SearchCityPartAttr`、`SearchInnerObjIDs`、`SearchNearObjIDs`、`SearchOuterObjIDs`、`SelectionObjInnerInsertPoint`

`SelectionObjMerge`、`SelectionObjPartZ`、`SelectionObjTopProcess`


### 批8 系统设置+图幅与打印（27 个：系统设置 / 图幅与打印）

`AddDispFilterInfo`、`ClearDispFilterInfo`、`GetFeatureCodeTB`、`GetLayerCount`、`GetLayerName`

`GetLayerStatus`、`GetMapStatus`、`GetNoteTemplateTB`、`GetSymbolScriptTB`、`RefreshView`

`SetFeatureCodeTB`、`SetLayerStatus`、`SetMapStatus`、`SetNotetemplateTB`、`SetUnfilterObjColor`

`CreateMapFrameByCenterLine`、`CreateMapFrameByRegion`、`CreateMapFrame`、`CreateOneMapFrame`、`FreeMapFrame`

`GetCurMapFrame`、`GetMapFrameCenterPoint`、`GetMapFrameCount`、`GetMapFramePoint`、`PrintCurFrameMap`

`PrintMap`、`SetCurMapFrame`


### 批9 选择集+地模+外部函数（23 个：选择集操作 / 地模处理 / 外部函数）

`ClearSelectConditionGroups`、`ClearSelectConditionGroup`、`ClearSelectCondition`、`ClearSelection`、`ClearSysSelection`

`SelectFilter`、`SetSelectConditionGroup`、`SetSelectCondition`、`UpdateSysSelection`、`CreateDem`

`CreateDgx`、`CreateTriNetOutPolygon`、`CreateTriNet`、`DeleteDem`、`DeleteTriNet`

`ExportDem`、`TransTriNetToGeo`、`AddFunctionParameter`、`ClearFunctionParameter`、`ExecuteCommand`

`LoadXLibrary`、`UnLoadXLibrary`、`外部`


### 批10 工程管理+数据转换+数据检查+坐标转换+角度（19 个：工程管理 / 数据转换 / 数据检查 / 坐标转换 / 角度函数）

`CloseDatabase`、`CreateDatabase`、`OpenDatabase`、`SelectFileName`、`SelectPathName`

`ClearDataXParameter`、`ExportData`、`GetImportFileName`、`ImportData`、`SetDataXParameter`

`AddCheckRecord`、`LoadCheckRecord`、`RemoveCheckRecord`、`SaveCheckRecord`、`ShowCheckOutput`

`TransCoordXYZ`、`TransSelectionObjCoord`、`XYSA`、`角度转换`


## 3. 交叉核对结论（CSV 解析产物 vs 官方帮助目录）

- CSV `eps_api_catalog.csv` 的 SSProcess 类 335 个；官方帮助目录 `eps_script_help\...\SSProcess对象参考\` 13 个子目录共 **174 个函数页面**。
- 交集 126 个；**官方独有 48 个**（CSV 解析遗漏，本批已从官方目录全量补齐），含关键 API：`ExecuteCommand`（外部模块接口=WzExtBridge 签名来源）、`ExecuteSql`、`ImportMdbTable`、`ExportSelectionToAccessTable`、`MergeNearGeoObj`、`SelectionObjMerge` 等。
- CSV 独有 209 个：属 CHM 全文档其他章节/对象归类宽泛，非 SSProcess 帮助目录内容，记为**已知差异**（不影响官方函数覆盖完整性）。
- **官方 SSProcess 174 个函数 100% 覆盖**；批1-5 小对象类 80 个 + 批6-10 共 254 个函数完成分析。

## 4. 证据与复现

- 产物：`stage31_batch6_db.json` / `batch7_arrange` / `batch8_sys_print` / `batch9_sel_dem_ext` / `batch10_misc`（每函数：名称/签名/说明/原文摘录）
- 复现：`stage31_batch6to10.py`（官方目录直读 GB2312 解析）

## 5. 阶段3.1 结论

- 官方文档函数层全量覆盖：SSProcess 174 + SSFunc 26 + SSProject 26 + SSView 14 + SSArray 10 + SSParameter 4 = **254 个**（含批1-3 合计）。
- 模块切片文档完备：对象体系架构、四层句柄体系、13 个子类归组、SDL 命令链路归属（步骤2.4 闭环）。
- 与自动化主线的衔接点全部落位：绘制/属性/SQL/进程治理/外部模块接口均有官方签名对照。